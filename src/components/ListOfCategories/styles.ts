import styled from 'styled-components';

export const List = styled.ul`
  display: flex;
  flex-direction: row;
  overflow: scroll;
  width: 100%;
  height: 100%;
  list-style: none;
  padding: 0px;
`;

export const ListItem = styled.li`
  padding: 0rem 0.5rem;
`;
